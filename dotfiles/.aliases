# Easier navigation
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias countryroads='cd ~'

# Software Binaries
alias intpy='/usr/local/bin/python3'
alias intpip='/usr/local/bin/pip3'

alias vpy='.venv/bin/python3'
alias vpip='.venv/bin/pip3'

alias brow='/usr/local/bin/brew'
alias brew='/opt/homebrew/bin/brew'
alias func='arch -x86_64 /usr/local/bin/func'

# Always enable colored `grep` output
alias grep='grep --color=auto '

# Pipe my public key to my clipboard.
alias pubkey="cat ~/.ssh/id_rsa.pub | pbcopy | echo '=> Public key copied to pasteboard.'"

# Pipe my private key to my clipboard.
alias privkey="cat ~/.ssh/id_rsa | pbcopy | echo '=> Private key copied to pasteboard.'"

# Paths
eval "$(/opt/homebrew/bin/brew shellenv)"
